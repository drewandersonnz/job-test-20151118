#!/bin/bash

echo "For safety reasons I will print this and not action it."
echo "Please execute the below command, edit the path to the aws.pem file:"
echo
echo "sudo cp /etc/ansible/hosts /etc/ansible/hosts.backup"
echo "sudo cp ./tmp/hosts /etc/ansible/hosts && sudo nano /etc/ansible/hosts"
echo
